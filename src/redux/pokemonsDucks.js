import axios from 'axios';

// Constants
const initialData = {
  array: [],
  pokemons: [],
}

// Types

// Reducer
export default function pokemonReducer(state = initialData, action) {
  return state
}

// Actions

